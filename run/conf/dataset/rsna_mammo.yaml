type: rsna_mammo

num_classes: 1
num_folds: 5
test_fold: 0  # Test fold for N-fold CV. Should be in [0, num_folds).
val_fold: ${dataset.test_fold}  # Validation fold for N-fold CV. Should be in [0, num_folds).
seed: 2023
num_records: 0
phase: "train"
augmentation:
  p_crop_resize: 0.75
  bbox_size_scale_min: 0.9
  bbox_size_scale_max: 1.1
use_cache: false
